<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bitc.next502.team_prj.mapper.ReservationMapper">

    <insert id="insertReservation" parameterType="bitc.next502.team_prj.dto.ReservationDTO">
        INSERT INTO reservation (
            reservation_id,
            user_id,
            restaurant_id,
            reservation_date,
            reservation_time,
            reservation_state,
            count_people
        )
        VALUES (
                   #{reservationId},
                   #{userId},
                   #{restaurantId},
                   #{reservationDate},
                   #{reservationTime},
                   '방문예정',           #{countPeople}
               )
    </insert>

</mapper>